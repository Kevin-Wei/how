<p id="notice"><%= notice %></p>

<p>
  <b>Order:</b>
  <%= @step.order %>
</p>

<p>
  <b>Todo:</b>
  <%= @step.todo %>
</p>

<%= link_to 'Edit', edit_task_step_path(@task,@step) %> |
<%= link_to 'Delete', [@task,@step], :method => :delete, :confirm => "Are you sure?" %> |
<%= link_to 'Back', task_path(@task) %>
